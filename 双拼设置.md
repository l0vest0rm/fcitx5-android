如何让fcitx5-android编译出来的默认带双拼输入法
要让 fcitx5-android 编译后默认带双拼输入法，需完成两个核心配置：将双拼添加到默认输入法列表并预启用双拼模式，同时通过编译脚本将这些配置打包进 APK。以下是完整可落地的步骤：
一、核心原理与准备工作
fcitx5-android 的默认配置由两部分控制：
输入法列表配置（profile 文件）：决定默认显示哪些输入法
拼音引擎配置（pinyin.config）：控制拼音 / 双拼的默认行为
准备工作：
克隆 fcitx5-android 仓库并初始化子模块：
bash
运行
git clone https://github.com/fcitx5-android/fcitx5-android.git
cd fcitx5-android
git submodule update --init --recursive
确保已安装所需依赖（Android SDK/NDK、extra-cmake-modules、gettext 等）
二、修改默认输入法列表（profile 文件）
创建默认 profile 配置文件在app/src/main/assets/fcitx5目录下创建profile文件（如不存在需新建目录），内容如下：
ini
[Groups/0]
Name=Default
DefaultIM=pinyin

[Groups/0/Items/0]
Name=keyboard-us

[Groups/0/Items/1]
Name=pinyin
Configurable=True

[GroupOrder]
0=Default
关键说明：
DefaultIM=pinyin：设置默认激活的输入法为拼音（双拼基于拼音引擎）
Items/1添加拼音输入法（双拼是拼音的子模式）
配置编译时自动复制 profile 文件修改app/build.gradle.kts，添加资产复制规则：
kotlin
android {
    // ... 其他配置
    sourceSets {
        main {
            assets.srcDirs("src/main/assets", "src/main/assets/fcitx5")
        }
    }
}
三、预启用双拼模式（修改拼音引擎默认配置）
创建拼音默认配置文件在app/src/main/assets/fcitx5/conf目录下创建pinyin.config，内容如下：
ini
[Pinyin]
Shuangpin=True  # 核心配置：默认启用双拼
ShuangpinSchema=NaturalCode  # 默认双拼方案（可选：NaturalCode/ Xiaohe/ Microsoft）
DisplayShuangpinMode=True  # 显示当前双拼模式

# 可选：其他拼音配置
AutoSelect=True
AutoPhrase=True
双拼方案说明：
NaturalCode：自然码（默认）
Xiaohe：小鹤双拼
Microsoft：微软双拼
配置编译时复制拼音配置同样在app/build.gradle.kts中添加 conf 目录到资产：
kotlin
android {
    sourceSets {
        main {
            assets.srcDirs("src/main/assets", "src/main/assets/fcitx5", "src/main/assets/fcitx5/conf")
        }
    }
}
四、修改编译脚本确保配置生效
确保预配置文件在初始化时被正确加载修改app/src/main/java/org/fcitx/fcitx5/android/FcitxApplication.kt（或相关初始化类），添加配置加载逻辑：
kotlin
override fun onCreate() {
    super.onCreate()
    // 确保assets中的fcitx5配置被复制到应用数据目录
    val assetsManager = assets
    val configDir = filesDir.resolve("fcitx5")
    if (!configDir.exists()) {
        copyAssetsFolder(assetsManager, "fcitx5", configDir.absolutePath)
    }
}

// 实现assets复制函数
private fun copyAssetsFolder(assets: AssetManager, src: String, dst: String) {
    // 实现文件复制逻辑...
}
该步骤确保首次启动时，预配置文件被复制到 fcitx5 的工作目录，覆盖默认行为。
（可选）修改拼音引擎默认设置如需更彻底的默认设置，可修改fcitx5-chinese-addons子模块中的拼音默认配置：
路径：subprojects/fcitx5-chinese-addons/im/pinyin/pinyin.conf.in
添加Shuangpin=True到[Pinyin]部分
重新编译子模块：./gradlew :subprojects:fcitx5-chinese-addons:assemble
五、编译与验证
编译 APK
bash
运行
./gradlew assembleDebug  # 或assembleRelease
验证默认双拼功能
安装编译后的 APK 并启用为系统输入法
首次打开键盘应默认进入拼音输入模式
检查设置界面：输入法配置 → 拼音 → 双拼（应显示为已启用）
六、常见问题与优化
双拼方案自定义如需默认使用小鹤双拼，修改pinyin.config中的ShuangpinSchema=Xiaohe
配置不生效排查
检查 assets 目录结构是否正确（fcitx5/profile和fcitx5/conf/pinyin.config）
清除应用数据后重新测试（确保首次启动加载新配置）
查看 logcat 日志，确认 fcitx5 是否成功加载配置文件
编译优化可将配置文件打包进单独的 asset 模块，方便管理多版本配置
总结
通过预配置 profile 文件和拼音引擎设置，并在编译时将这些配置打包进 APK，即可实现 fcitx5-android 默认带双拼输入法。核心是利用 fcitx5 的配置系统，在应用首次启动前就完成双拼模式的启用，无需用户手动设置。
需要我提供一份可直接复制的 profile 和 pinyin.config 示例文件，以及 build.gradle.kts 的完整修改片段吗？
